{{!-- ADD ACTIVE STYLE LATER BTW!!!! --}}

<nav class="bg-white shadow-md">
    <div class="max-w-7xl mx-auto px-4">
        <div class="flex justify-between h-16 items-center">
            <nav class="flex space-x-4">
                <a href="/home" class="{{#if navHome}}text-blue-500 font-bold {{else}}text-gray-700 hover:text-blue-500 {{/if}} px-3 py-1 rounded-lg transition">Home</a>
                <a href="/items" class="{{#if navItems}}text-blue-500 font-bold {{else}}text-gray-700 hover:text-blue-500 {{/if}} rounded-lg px-3 py-1 transition">Items</a>
                <a href="/checkout" class="{{#if navCheckout}}text-blue-500 font-bold {{else}}text-gray-700 hover:text-blue-500 {{/if}} rounded-lg px-3 py-1 transition">Checkout</a>
                <a href="/report" class="{{#if navReport}}text-blue-500 font-bold {{else}}text-gray-700 hover:text-blue-500 {{/if}} rounded-lg px-3 py-1 transition">Report</a>
                {{!-- ADD IF CONDITION TO CHECK FOR THIS + KEY TO CHECK IF PAGE IS ALLOWED  --}}
                <a href="/users" class="{{#if navUsers}}text-blue-500 font-bold {{else}}text-gray-700 hover:text-blue-500 {{/if}} p-3 py-1 rounded-lg transition">Users</a>
            </nav>
            <div>
                <a href="/logout" 
                class="px-4 py-2 border-2 border-red-500 text-red-500 rounded-lg font-semibold hover:bg-red-500 hover:text-white transition">
                Logout
                </a>
            </div>
        </div>
    </div>
</nav>