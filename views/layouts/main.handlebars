<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Group 6 Final Project</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-gray-100">

  <!-- NAVBAR -->
  <nav class="bg-white shadow-md">
    <div class="max-w-7xl mx-auto px-4">
      <div class="flex justify-between h-16 items-center">
        <div class="flex space-x-4">
            <a href="/home" class="{{isActive 'home' activePage}}">Home</a>
            <a href="/items" class="{{isActive 'items' activePage}}">Items</a>
            <a href="/users" class="{{isActive 'users' activePage}}">Users</a>
        </div>
        <div>
            <a href="/logout" 
            class="px-4 py-2 border-2 border-red-500 text-red-500 rounded-lg font-semibold hover:bg-red-500 hover:text-white transition">
            Logout
            </a>
        </div>
      </div>
    </div>
  </nav>

  <!-- MAIN CONTENT -->
  <div class="mt-6">
    {{{body}}}
  </div>

</body>
</html>